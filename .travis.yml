language: cpp
compiler: g++
dist: xenial

sudo: required

script: 
  - sudo apt-get update
  - make

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-multilib
    - gcc-multilib
    - binutils
